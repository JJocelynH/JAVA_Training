**

1. jdbc vs hibernate
		jdbc is a low level java api where you write raw SQL yourself and manually handle everything, for example opening connections, setting parameters etc. Hibernate is an ORM that sits on top of JDBC and maps java objects to database tables automatically, so you work with objects instead of SQL and it generates the queries for you
		The tradeoff is control vs convenience. JDBC gives you full control over what SQL runs, which makes it easier to optimize. HIbernate eliminates boilerplate and handles relationships and caching nicely, but generated SQL can be inefficient and harder to debug.    
		
2. Statement vs PreparedStatement vs CallableStatement
		All three are JDBC interfaces for executing SQL.
		Statement is the simplest, you just pass a raw SQL string directly. The problem is you have to concatenate values into the string yourself. It will opens you up to SQL injection and means the database reparses and recompiles the query every time it runs.
		PreparedStatement solves both problems. You write the SQL once with ? placeholders, and the database pre-compiles it. Then set values saparately with typed setter like setInt() or setString(). This makes it safe from injection, more readable and faster when the same query runs multiple times since the execution plan is cached
		CallableStatement extends PreparedStatement for calling stored procedures in the database.It supports both IN parameters (values you pass in) and OUT parameters (values the procedure returns back to you),
    
3. how to prevent sql injection
	The main way is to use **PreparedStatement** with `?` placeholders instead of concatenating user input into the SQL string directly. The database treats the parameter values as pure data, never as executable SQL, so even if someone passes in malicious input it just gets treated as a literal string.
	
	Do Input validation to reject input that doesn't match what you expect.
	
	**Least privilege** — the database user your app connects with should only have the permissions it actually needs. If it only needs to read data, don't give it DELETE or DROP rights. That way even a successful injection can't do as much damage.
	
	**ORM** — using Hibernate or Spring Data JPA handles parameterization for you automatically, so you get the same protection without thinking about it manually.
    
1. what is ORM
	ORM stands for object-relational mapping. It's a technique that lets you work with database data as Java objects instead of writing raw SQL. The core idea is your database hans tables and rows, your java code has classes and objects. ORM maps between the two automatically.
	Hibernate is the most common ORM framework in Java, and Spring Data JPA builds on top of it to make things even simpler.
    
2. JPA vs Hibernate
	JPA（java persistence api) is a specification. It is a set of rules and interfaces that define how ORM should work in Java. It defines annotations like @Entity, @Id, @OneToMany, and interfaces like EntityManager, without saying how they're implemented under the hood.
	Hibernate is one of several implementations of that specification. It's the actual code that does the work. When you use @Entity form JPA, Hibernate is the engine that reads that annotation and generates the real SQL to make it happen.
    
3. what are the Persistent State in Entity LifeCycle
	In JPA/Hibernate, an entity moves through four lifecycle states: Transient, Persistent, Detached and Removed.
	Transient means the object was just created with new and has no connection to the database at all.
	Persistent means the entity is attached to an active session and the database is actively tracking it, so any changes you make are automatically detected and saved to the database when the transaction commits.
	Detached means the entity used to be Persistent but the session has already closed, so it still has a database identity but changes to it are no longer automatically tracked.
	 Removed means the entity has been marked for deletion and will be deleted from the database once the transaction commits.
    
4. Mapping relationship
	In JPA/Hibernate, mapping relationships define how entities relate to each other, mirroring foreign keys in the database.
	**@OneToOne** — one entity maps to exactly one of another

	**@OneToMany** — one entity maps to many of another

	**@ManyToOne** — many entities map to one. This is usually the owning side, since it holds the foreign key.

	**@ManyToMany** — many entities map to many of another, usually requiring a join table.
    
5. what is the cascade type
	**CascadeType** controls whether operations performed on a parent entity automatically propagate to its related child entities,
	The main cascade types are:

	**PERSIST** — saving the parent also saves the child entities.

	**MERGE** — updating the parent also updates the child entities.

	**REMOVE** — deleting the parent also deletes the child entities.

	**REFRESH** — reloading the parent from the database also reloads the child entities.
	
	**DETACH** — detaching the parent from the session also detaches the child entities.
	
	**ALL** — applies all of the above cascade behaviors at once.

	You set this in the relationship annotation, like `@OneToMany(cascade = CascadeType.ALL)`, and it's something to use carefully — overusing cascade, especially REMOVE, can accidentally delete more data than you intended.
    
6. what is the fetch type
	**FetchType** controls _when_ related entities are loaded from the database. it decides whether Hibernate grabs the related data immediately or waits until you actually need it.
	**EAGER** means the related entity is loaded immediately, at the same time as the parent
	**LAZY** means the related entity is only loaded when you actually access it in code
	LAZY is generally preferred for performance, since it avoids loading data you don't need — but it can cause the classic **LazyInitializationException** if you try to access the related entity after the session/EntityManager has already closed. EAGER avoids that problem but can lead to loading way more data than necessary, especially with large collections, which is part of what causes the N+1 query problem we mentioned earlier.
    
7. what is the first/second level cache
	**First-level cache** is built into Hibernate and tied to a single `Session`/`EntityManager` — it's automatically enabled and can't be turned off. Within one session, if you query the same entity twice, Hibernate returns the cached object the second time instead of hitting the database again, but this cache is cleared once the session closes.
	
	**Second-level cache** is optional and shared across multiple sessions — it sits at the `SessionFactory` level, so data cached from one session can be reused by a completely different session, even after the original session has closed. You have to explicitly enable it and configure a caching provider (like Ehcache or Redis) to use it.
	
	The key difference is scope and lifespan: first-level cache is short-lived and private to one session, while second-level cache is long-lived and shared application-wide, which makes it more useful for data that doesn't change often, like reference tables.
    
6. left join vs right join vs inner join vs outer join vs cross 
	**INNER JOIN** returns only the rows that have matching values in both tables — if there's no match, the row is excluded entirely.

	**LEFT JOIN** returns all rows from the left table plus matching rows from the right table — if there's no match, the right side just shows NULL.
	
	**RIGHT JOIN** is the mirror of LEFT JOIN — it returns all rows from the right table plus matching rows from the left table, filling NULL where there's no match.
	
	**FULL OUTER JOIN** returns all rows from both tables, matching where possible and filling NULL on whichever side has no match
	
	**CROSS JOIN** returns every possible combination of rows from both tables (a Cartesian product)
    
7. union vs union all
	
	**UNION** combines the results of two queries into one table and automatically removes duplicate rows — if the same row appears in both queries, only one copy is kept.
	
	**UNION ALL** also combines the results of two queries, but it doesn't remove duplicates — even if the exact same row appears in both queries, it shows up multiple times in the final result.

**